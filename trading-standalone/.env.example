# ===========================================
# Magellan Trading Standalone - Environment Variables
# ===========================================
# 
# ⚠️ 重要说明：
# 1. 优先从主项目根目录 ../.env 读取 API Keys (如 GOOGLE_API_KEY, OKX_* 等)
# 2. 本文件仅用于覆盖或添加独立交易系统的特定配置
# 3. 配置优先级: 本文件 > config.yaml > 默认值
#
# 如果主项目 .env 已配置好，你只需复制下面少量配置即可启动
# cp .env.example .env

# ========== 交易系统特定配置 ==========

# LLM 提供商选择 (trading-standalone 推荐使用 deepseek)
# ⚠️ 重要: Gemini 对复杂工具调用支持较差，交易建议用 deepseek
DEFAULT_LLM_PROVIDER=deepseek

# 是否使用 OKX 模拟盘交易
# true = OKX 模拟盘, false = 本地 PaperTrader
USE_OKX_TRADING=true

# ========== 以下配置一般在 config.yaml 中调整 ==========
# 如果需要覆盖 config.yaml, 可在此处设置

# 分析间隔 (小时)
# SCHEDULER_INTERVAL_HOURS=4

# 最大杠杆
# MAX_LEVERAGE=20

# 仓位百分比
# MAX_POSITION_PERCENT=30
# MIN_POSITION_PERCENT=10
# DEFAULT_POSITION_PERCENT=20

# 止盈止损
# DEFAULT_TP_PERCENT=5.0
# DEFAULT_SL_PERCENT=2.0

# ========== 邮件通知 (可选) ==========
EMAIL_ENABLED=false
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASSWORD=your_app_password
# EMAIL_FROM=your_email@gmail.com
# EMAIL_TO=recipient@example.com

# ========== 日志级别 ==========
LOG_LEVEL=INFO
