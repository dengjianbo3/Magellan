# Core web framework
fastapi
uvicorn[standard]

# For making HTTP requests to other services
httpx

# For handling file uploads from the client
python-multipart

# For environment variable management (good practice)
pydantic-settings

# For Redis session storage
redis>=5.0.0

# For PDF report generation (Phase 2)
reportlab>=4.0.0
pillow>=10.0.0  # Image processing for PDF

# For Word document generation (Phase 2)
python-docx>=1.0.0

# For Excel data export (Phase 2)
openpyxl>=3.1.0

# For data visualization and charts (Phase 2)
matplotlib>=3.8.0
seaborn>=0.13.0  # Statistical data visualization

# For system monitoring (Phase 2)
prometheus-fastapi-instrumentator>=7.0.0  # Prometheus metrics
structlog>=24.0.0  # Structured logging
python-json-logger>=2.0.7  # JSON log formatting

# For knowledge base and vector search (Phase 2)
qdrant-client>=1.7.0  # Qdrant vector database client
sentence-transformers>=2.2.0  # For text embeddings
pypdf>=3.17.0  # PDF text extraction
python-docx>=1.1.0  # Word document parsing (already added above for export)
rank-bm25>=0.2.2  # BM25 ranking for hybrid search

# For financial data retrieval (Phase 3)
yfinance>=0.2.40  # Yahoo Finance API wrapper
